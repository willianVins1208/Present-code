$kind: http-request
url: "{{baseUrl}}/api/items/{{itemId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("deleted item status is 404", function () {
        pm.response.to.have.status(404);
      });
    language: text/javascript
order: 13000
