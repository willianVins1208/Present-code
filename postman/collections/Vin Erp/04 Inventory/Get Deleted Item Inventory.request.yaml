$kind: http-request
url: "{{baseUrl}}/api/items/{{itemId}}/inventory"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("deleted item inventory status is 404", function () {
        pm.response.to.have.status(404);
      });
    language: text/javascript
order: 14000
