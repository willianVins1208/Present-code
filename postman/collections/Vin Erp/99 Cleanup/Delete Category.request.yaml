$kind: http-request
url: "{{baseUrl}}/api/categories/{{categoryId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test("category delete status is 204", function () {
        pm.response.to.have.status(204);
      });
    language: text/javascript
order: 15000
